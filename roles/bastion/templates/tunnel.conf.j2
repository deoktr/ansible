{{ ansible_managed | comment }}

Match Group {{ bastion_jump_group_name }}
  # TODO create chroot for each users
  # ChrootDirectory %h
  PermitTunnel yes
  AllowTcpForwarding yes
  # users can still proxy jump but can't get a shell on this server
  ForceCommand /bin/false
