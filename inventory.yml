servers:
  hosts:
    sys_log_01:
      ansible_host: syslog
      ansible_user: ansible
      ansible_become_pass: 73t:W9V5EONu_3J9JFqa
      rsyslog_server_host:  # itself
      rsyslog_enable_tcp_reception: true
      rsyslog_enable_udp_reception: false


bastions:
  hosts:
    bastion_01:
      ansible_host: bastion
      ansible_user: "{{ bastion_ansible_user }}"
      ansible_become_pass: "{{ bastion_ansible_become_pass }}"
      users: "{{ bastion_users }}"
      sudo_users:  # nobody should be sudoer on the bastion !
      sshd_from_ip: 192.168.0.0/16
      pass_max_days: 30


update_servers:
  hosts:
    maj_lin_01:
      ansible_host: maj_lin
      ansible_user: ansible
      ansible_become_pass: D6oM8wLPGta51,DgXp_y
      apt_proxy_addr:  # itself
      use_apt_proxy: false  # itself
