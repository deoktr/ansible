skip_list:
  - package-latest  # Package installs should not use latest.

exclude_paths:
  - ../roles/suricata/files/suricata.yaml
  - ../roles/motd/defaults/main.yml
  - ../roles/bastion/tasks/chroot.yml
